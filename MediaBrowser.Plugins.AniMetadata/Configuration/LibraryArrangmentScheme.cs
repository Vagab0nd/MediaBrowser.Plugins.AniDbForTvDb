namespace MediaBrowser.Plugins.AniMetadata.Configuration
{
    public enum LibraryArrangmentScheme
    {
        /// <summary>
        ///     Multiple series with one season and absolute episode numbers
        /// </summary>
        AniDb,

        /// <summary>
        ///     Single series with seasons
        /// </summary>
        TvDb
    }
}